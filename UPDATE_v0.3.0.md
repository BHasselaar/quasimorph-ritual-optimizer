# Update to v0.3.0

Replace the matching repository files with the contents of the changed-files archive. Delete these obsolete files if present:

- `src/quasimorph_optimizer/calibration.py`
- `src/quasimorph_optimizer/data/default_observations.csv`
- `tests/test_calibration.py`
- `UPDATE_v0.2.0.md`

Your `%APPDATA%\QuasimorphRitualOptimizer\inventory.csv` is preserved. On startup, the exact legacy Spider Joint row is migrated from Shavva to Agga automatically.
